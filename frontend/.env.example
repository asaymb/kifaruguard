# Build / dev Vite — copier vers frontend/.env.local si besoin
# Vide = requêtes relatives (Nginx proxy en Docker).

VITE_API_BASE_URL=

# WebSocket HITL (optionnel ; défaut = même host que la page)
# VITE_WS_BASE_URL=
